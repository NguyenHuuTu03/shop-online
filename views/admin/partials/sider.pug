.sider
  .inner-menu
    ul.list-unstyled.mb-0

      li
        a(href=prefixAdmin + "/dashboard")
          i.fa-solid.fa-gauge-high
          span Tổng quan
      
      if(role && role.permissions && role.permissions.includes("product_view"))
        li
          a(href=prefixAdmin + "/products")
            i.fa-solid.fa-box-open
            span Sản phẩm
      if(role && role.permissions && role.permissions.includes("category_view"))
        li
          a(href=prefixAdmin + "/categories")
            i.fa-solid.fa-layer-group
            span Danh mục sản phẩm
      if(role && role.permissions && role.permissions.includes("user_view"))
        li
          a(href=prefixAdmin + "/users")
            i.fa-solid.fa-user
            span Quản lý người dùng
      if(role && role.permissions && role.permissions.includes("order_view"))
        li
          a(href=prefixAdmin + "/orders")
            i.fa-solid.fa-cart-shopping.me-2
            span Quản lý đơn hàng
      if(role && role.permissions && role.permissions.includes("deliver_view"))
        li
          a(href=prefixAdmin + "/deliver")
            i.fa-solid.fa-truck-fast.me-2
            span Giao hàng
      if(role && role.permissions && role.permissions.includes("role_view"))
        li
          a(href=prefixAdmin + "/roles")
            i.fa-solid.fa-users-gear
            span Nhóm quyền
      if(role && role.permissions && role.permissions.includes("role_permissions"))
        li
          a(href=prefixAdmin + "/roles/permission")
            i.fa-solid.fa-key
            span Phân quyền
      if(role && role.permissions && role.permissions.includes("general"))
        li
          a(href=prefixAdmin + "/settings/general")
            i.fa-solid.fa-gear
            span Cài đặt chung